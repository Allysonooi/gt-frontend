export type FormData = {
  name: string;
  email: string;
  contactNo: string;
  agencyName: string;
  feedback: string;
};

export type FormItem = {
  label: string;
  keyName: string;
  required: boolean;
};

export type TableFormData = {
  email: string;
  contactNo: string;
};
